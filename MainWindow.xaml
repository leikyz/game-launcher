<Window x:Class="Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Launcher"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="Launcher" 
        Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="Made">
            <Setter Property="TextElement.FontFamily" Value="pack://application:,,,/Fonts/#Made Tommy" />
        </Style>
        <Style x:Key="Sectar">
            <Setter Property="TextElement.FontFamily" Value="pack://application:,,,/Fonts/#Sectar" />
        </Style>
        <Style x:Key="Hiro">
            <Setter Property="TextElement.FontFamily" Value="pack://application:,,,/Fonts/#HIRO MISAKE" />
        </Style>
        <Style x:Key="DownloadButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="Black" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Height" Value="50" />
            <Setter Property="Width" Value="269" />
            <Setter Property="Content" Value="Download" />
            <Setter Property="Margin" Value="0 78 0 0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontStyle" Value="Normal" />
            <Setter Property="FontStretch" Value="UltraCondensed" />
            <Setter Property="Foreground" Value="#C0C0C0" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="Black" Opacity="0.7"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="BorderBrush">
                                    <Setter.Value>
                                        <SolidColorBrush Color="White" Opacity="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="Black" Opacity="0.9"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="Black" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="Gray" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageHoverScale" TargetType="{x:Type Image}">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.2" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Width" Value="35" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.7" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MainActionButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="#99FFFFFF" />
            <Setter Property="Foreground" Value="#FF222222" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Style" Value="{StaticResource Made}" />
            <Setter Property="Height" Value="40" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCFFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CCFFFFFF" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#66AAAAAA" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#99AAAAAA" />
                                <Setter Property="Foreground" Value="#FF555555" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" BorderThickness="0" Background="{TemplateBinding Background}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter Content="{TemplateBinding Tag}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5,0,0" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Grid.ColumnSpan="2" Margin="-6,-10,447,-10">
            <Border.Background>
                <ImageBrush ImageSource="/Resources/side.jpg" Stretch="UniformToFill" Opacity="1"/>
            </Border.Background>
            <Grid Margin="0,0,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>



                <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="30,113,61,0">


                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>



                    </Grid>

                    <Button x:Name="downloadButton" Style="{StaticResource DownloadButtonStyle}" FontSize="25" FontWeight="Black" HorizontalAlignment="Left" Height="50" Width="269" Click="DownloadButton_Click" Content="Download" Margin="0 78" Cursor="Hand" FontStyle="Normal" FontStretch="UltraCondensed" Grid.Column="1" Foreground="#C0C0C0">
                        <Button.BorderBrush>
                            <SolidColorBrush Opacity="0" Color="White"/>
                        </Button.BorderBrush>
                        <Button.Background>
                            <SolidColorBrush Color="Black" Opacity="0.7"/>
                        </Button.Background>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,28,0">
                    <Button x:Name="btnSite" Style="{StaticResource LinkButtonStyle}" Click="Site_Click" Width="80" Height="70" Margin="10,0">
                        <Button.Tag>
                            <Image Source="/Resources/site.png" Stretch="Uniform" Height="30" Width="30" Style="{StaticResource ImageHoverScale}"/>
                        </Button.Tag>
                        <Button.Content>
                            <TextBlock Text="Site Web" Foreground="#E0E0E0" FontSize="12" Style="{StaticResource Made}" HorizontalAlignment="Center"/>
                        </Button.Content>
                    </Button>
                    <Button x:Name="btnDiscord" Style="{StaticResource LinkButtonStyle}" Click="Discord_Click" Width="80" Height="70" Margin="10,0">
                        <Button.Tag>
                            <Image Source="/Resources/discord.png" Stretch="Uniform" Height="30" Width="30" Style="{StaticResource ImageHoverScale}"/>
                        </Button.Tag>
                        <Button.Content>
                            <TextBlock Text="Discord" Foreground="#E0E0E0" FontSize="12" Style="{StaticResource Made}" HorizontalAlignment="Center"/>
                        </Button.Content>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="3" VerticalAlignment="Bottom" Margin="10,10,30,5">
                    <Label x:Name="studioNameLabel" 
                           Visibility="Visible" 
                           Style="{StaticResource Sectar}" 
                           Content="{x:Static local:Config.GameStudioName}" 
                           HorizontalAlignment="Center"
                           FontSize="12" 
                           FontWeight="Bold" 
                           Foreground="#a69065"/>

                    <Label x:Name="versionLabel" 
                           Visibility="Visible" 
                           Style="{StaticResource Sectar}" 
                           Content="{x:Static local:Config.Version}" 
                           HorizontalAlignment="Center"
                           FontSize="10" 
                           FontWeight="Normal" 
                           Foreground="#a69065"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="#222222">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                <Button Name="btnPar" Style="{StaticResource NavigationButtonStyle}" Click="Settings_Click">
                    <Image Source="pack://application:,,,/Resources/newgear.png" Stretch="Uniform" Height="20" Width="20" />
                    <Button.ToolTip>
                        <ToolTip Content="Settings" />
                    </Button.ToolTip>
                </Button>
                <Button Name="btnMinus" Style="{StaticResource NavigationButtonStyle}" Click="minusButton_Click">
                    <Image Source="pack://application:,,,/Resources/minus.png" Stretch="Uniform" Height="20" Width="20" />
                </Button>
                <Button x:Name="btnClose" Style="{StaticResource NavigationButtonStyle}" Click="closeButton_Click">
                    <Image Source="pack://application:,,,/Resources/newclose.png" Stretch="Uniform" Height="20" Width="20" />
                </Button>
            </StackPanel>
            <TextBlock Text="NEWS &amp; UPDATES" Style="{StaticResource Made}"
                       Foreground="#E0E0E0" FontSize="24" FontWeight="Bold"
                       Margin="20,15,20,10" HorizontalAlignment="Left"
                       Grid.Row="0"/>

            <Border Grid.Row="1" 
                    Background="#FF2A2A2A" 
                    CornerRadius="3" 
                    Margin="20,0,20,10"
                    BorderBrush="#FF444444" 
                    BorderThickness="1"
                    Cursor="Hand">
                <StackPanel>
                    <Image Source="/Resources/bignews.png" Height="144" Stretch="UniformToFill" />
                    <StackPanel Margin="10">
                        <TextBlock Text="NEW ZOMBIE HORDES DETECTED!" Style="{StaticResource Made}" 
                                   Foreground="#FFD700" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
                        <TextBlock Text="Survivors report larger and more aggressive hordes in the abandoned city sectors. Gear up!" 
                                   Foreground="#E0E0E0" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        <TextBlock Foreground="#808080" FontSize="9" Margin="0,8,0,0"><Run Text="25/10/202"/><Run Language="fr-fr" Text="5"/></TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="20,0,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" 
                        Background="#FF2A2A2A" 
                        CornerRadius="3" 
                        Margin="0,0,5,0"
                        BorderBrush="#FF444444" 
                        BorderThickness="1"
                        Cursor="Hand">
                    <StackPanel>
                        <Image Source="/Resources/news.png" Height="80" Stretch="UniformToFill" />
                        <StackPanel Margin="8">
                            <TextBlock Text="EXCLUSIVE WEAPON SKIN DROPS!" Style="{StaticResource Made}" 
                                       Foreground="#A0FFFF" FontSize="12" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Foreground="#808080" FontSize="8"><Run Language="fr-fr" Text="20/10/2025"/></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" 
                        Background="#FF2A2A2A" 
                        CornerRadius="3" 
                        Margin="5,0,0,0"
                        BorderBrush="#FF444444" 
                        BorderThickness="1"
                        Cursor="Hand">
                    <StackPanel>
                        <Image Source="/Resources/news.png" Height="80" Stretch="UniformToFill" />
                        <StackPanel Margin="8">
                            <TextBlock Text="SERVER MAINTENANCE " Style="{StaticResource Made}" 
                                       Foreground="#FFA0A0" FontSize="12" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Foreground="#808080" FontSize="8"><Run Language="fr-fr" Text="18/10/2025"/></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
        <ProgressBar x:Name="progressBar" Panel.ZIndex="-1" HorizontalAlignment="Left" Height="52" VerticalAlignment="Top" Width="271" Value="100" Margin="23,190,0,0">
            <ProgressBar.Foreground>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#000000cc"/>
                    <GradientStop Color="#000000cc" Offset="1"/>
                    <GradientStop Color="#000000cc" Offset="0.523"/>
                </LinearGradientBrush>
            </ProgressBar.Foreground>
            <ProgressBar.BorderBrush>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#FF1D1C1C" Offset="1"/>
                </LinearGradientBrush>
            </ProgressBar.BorderBrush>
            <ProgressBar.Background>
                <SolidColorBrush Color="#FF191919" Opacity="0.06"/>
            </ProgressBar.Background>
        </ProgressBar>
        <Label x:Name="dlPercent"
           Content="0%" 
           VerticalAlignment="Top"
           Foreground="#C0C0C0" 
           FontFamily="Arial" 
           FontSize="12" 
           Margin="284,170,9,0"/>
        <Label x:Name="infoUpdate" 
      Style="{StaticResource Made}" 
      Content="Loading..." 
      FontSize="14" 
      FontWeight="Normal" 
      Foreground="#C0C0C0" Margin="23,167,36,253"/>
    </Grid>


</Window>